"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = scrollToTop;
var scrollInterval = void 0;

function scrollToTop(_target) {
  var scrollHeight = window.scrollY;
  var target = Math.ceil(_target);
  var step = (target - scrollHeight) / 200 * 15;
  var scrollCount = 0;
  if (scrollInterval) {
    clearInterval(scrollInterval);
  }
  scrollInterval = setInterval(function () {
    if (Math.abs(window.scrollY - target) > Math.abs(step)) {
      scrollCount += 1;
      window.scrollTo(0, Math.ceil(scrollHeight + step * scrollCount));
    } else {
      window.scrollTo(0, target);
      clearInterval(scrollInterval);
      scrollInterval = undefined;
    }
  }, 15);
}
module.exports = exports["default"];