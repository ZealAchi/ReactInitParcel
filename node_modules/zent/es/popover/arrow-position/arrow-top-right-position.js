import createPlacement from '../placement/create';
import createArrowPosition from './create';
var locate = function (anchorBoundingBox, containerBoundingBox, contentDimension, options) {
    var right = anchorBoundingBox.right, left = anchorBoundingBox.left, top = anchorBoundingBox.top;
    var middle = (left + right) / 2;
    var x = middle - (contentDimension.width - 15);
    var y = top - contentDimension.height - options.cushion;
    return createArrowPosition(x, y, "top-right");
};
var ArrowTopRightPosition = createPlacement(locate);
export default ArrowTopRightPosition;
