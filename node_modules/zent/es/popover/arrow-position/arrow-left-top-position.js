import createPlacement from '../placement/create';
import createArrowPosition from './create';
var locate = function (anchorBoundingBox, containerBoundingBox, contentDimension, options) {
    var x = anchorBoundingBox.left - contentDimension.width - options.cushion;
    var middle = (anchorBoundingBox.top + anchorBoundingBox.bottom) / 2;
    var y = middle - 9;
    return createArrowPosition(x, y, "left-top");
};
var ArrowLeftTopPosition = createPlacement(locate);
export default ArrowLeftTopPosition;
