import createPlacement from '../placement/create';
import createArrowPosition from './create';
var locate = function (anchorBoundingBox, containerBoundingBox, contentDimension, options) {
    var left = anchorBoundingBox.left, right = anchorBoundingBox.right, bottom = anchorBoundingBox.bottom;
    var middle = (left + right) / 2;
    var x = middle - (contentDimension.width - 15);
    var y = bottom + options.cushion;
    return createArrowPosition(x, y, "bottom-right");
};
var ArrowBottomRightPosition = createPlacement(locate);
export default ArrowBottomRightPosition;
