import createPlacement from '../placement/create';
import createArrowPosition from './create';
var locate = function (anchorBoundingBox, containerBoundingBox, contentDimension, options) {
    var right = anchorBoundingBox.right, top = anchorBoundingBox.top, bottom = anchorBoundingBox.bottom;
    var x = right + options.cushion;
    var middle = (top + bottom) / 2;
    var y = middle - (contentDimension.height - 9);
    return createArrowPosition(x, y, "right-bottom");
};
var ArrowRightBottomPosition = createPlacement(locate);
export default ArrowRightBottomPosition;
