import { __assign, __extends, __rest } from "tslib";
import * as React from 'react';
import { Component } from 'react';
import PopoverContext from './PopoverContext';
export function exposePopover(propName) {
    return function expose(Base) {
        var _a;
        return _a = (function (_super) {
                __extends(ExposePopover, _super);
                function ExposePopover() {
                    return _super !== null && _super.apply(this, arguments) || this;
                }
                ExposePopover.prototype.render = function () {
                    var _a;
                    var popover = (this.context || {})._zentPopover;
                    var registerDescendant = popover.registerDescendant, unregisterDescendant = popover.unregisterDescendant, others = __rest(popover, ["registerDescendant", "unregisterDescendant"]);
                    var context = (_a = {},
                        _a[propName] = others,
                        _a);
                    return React.createElement(Base, __assign({}, this.props, context));
                };
                return ExposePopover;
            }(Component)),
            _a.contextType = PopoverContext,
            _a;
    };
}
export default exposePopover('popover');
