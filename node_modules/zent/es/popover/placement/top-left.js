import createPlacement from './create';
var locate = function (anchorBoundingBox, containerBoundingBox, contentDimension, options) {
    var left = anchorBoundingBox.left, top = anchorBoundingBox.top;
    var x = left;
    var y = top - contentDimension.height - options.cushion;
    return {
        getCSSStyle: function () {
            return {
                position: 'absolute',
                left: Math.round(x) + "px",
                top: Math.round(y) + "px",
            };
        },
        name: 'position-top-left',
    };
};
var TopLeft = createPlacement(locate);
export default TopLeft;
