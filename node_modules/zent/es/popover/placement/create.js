import { __assign } from "tslib";
export default function createPlacement(locate) {
    var pos = function (prefix) {
        var args = [];
        for (var _i = 1; _i < arguments.length; _i++) {
            args[_i - 1] = arguments[_i];
        }
        var placement = locate.apply(void 0, args);
        if (!placement || !placement.name || !placement.getCSSStyle) {
            throw new Error('name and getCSSStyle is required for a placement');
        }
        var name = prefix + "-popover-" + placement.name;
        return __assign(__assign({}, placement), { toString: function () {
                return name;
            } });
    };
    pos.locate = locate;
    return pos;
}
