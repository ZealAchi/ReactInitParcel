import createPlacement from './create';
var locate = function (anchorBoundingBox, containerBoundingBox, contentDimension, options) {
    var left = anchorBoundingBox.left, bottom = anchorBoundingBox.bottom;
    var x = left;
    var y = bottom + options.cushion;
    return {
        getCSSStyle: function () {
            return {
                position: 'absolute',
                left: Math.round(x) + "px",
                top: Math.round(y) + "px",
            };
        },
        name: 'position-bottom-left',
    };
};
var BottomLeft = createPlacement(locate);
export default BottomLeft;
