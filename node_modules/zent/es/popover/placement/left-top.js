import createPlacement from './create';
var locate = function (anchorBoundingBox, containerBoundingBox, contentDimension, options) {
    var x = anchorBoundingBox.left - contentDimension.width - options.cushion;
    var y = anchorBoundingBox.top;
    return {
        getCSSStyle: function () {
            return {
                position: 'absolute',
                left: Math.round(x) + "px",
                top: Math.round(y) + "px",
            };
        },
        name: 'position-left-top',
    };
};
var LeftTop = createPlacement(locate);
export default LeftTop;
