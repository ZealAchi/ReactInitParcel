import * as React from 'react';
import { AlertTypes } from './types';
import { Omit } from 'utility-types';
export interface IAlertProps extends Omit<React.HTMLAttributes<HTMLDivElement>, 'title'> {
    type?: AlertTypes;
    loading?: boolean;
    outline?: boolean;
    title?: React.ReactNode;
    description?: React.ReactNode;
    extraContent?: React.ReactNode;
    closable?: boolean;
    closed?: boolean;
    onClose?: () => void;
    closeContent?: React.ReactNode;
}
interface IAlertState {
    closed: boolean;
}
export declare class Alert extends React.PureComponent<IAlertProps, IAlertState> {
    static highlightClassName: string;
    static defaultProps: {
        type: string;
        loading: boolean;
        outline: boolean;
        closable: boolean;
    };
    state: IAlertState;
    private get isControlled();
    private get closed();
    private onCloseHandler;
    private renderContent;
    private renderCloseNode;
    private renderIcon;
    render(): JSX.Element;
}
export default Alert;
