import * as React from 'react';
import { Icon } from '../../icon';
var ProgressInfo = function (props) {
    var type = props.type, percent = props.percent, format = props.format, state = props.state;
    if (state === 'success') {
        return React.createElement(Icon, { type: type === 'circle' ? 'check' : 'check-circle' });
    }
    if (state === 'exception') {
        return React.createElement(Icon, { type: type === 'circle' ? 'close' : 'close-circle' });
    }
    return React.createElement(React.Fragment, null, format(percent));
};
export default ProgressInfo;
