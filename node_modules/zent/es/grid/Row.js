import { __assign, __extends } from "tslib";
import * as React from 'react';
import { PureComponent } from 'react';
import classnames from 'classnames';
import Cell from './Cell';
import noop from '../utils/noop';
var Row = (function (_super) {
    __extends(Row, _super);
    function Row() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    Row.prototype.render = function () {
        var _a;
        var _b = this.props, prefix = _b.prefix, columns = _b.columns, data = _b.data, rowIndex = _b.rowIndex, rowClassName = _b.rowClassName, mouseOverRowIndex = _b.mouseOverRowIndex, onRowClick = _b.onRowClick, onRowMouseEnter = _b.onRowMouseEnter, fixed = _b.fixed, scroll = _b.scroll, fixedColumnsBodyRowsHeight = _b.fixedColumnsBodyRowsHeight, row = _b.row, _c = _b.rowProps, rowProps = _c === void 0 ? noop : _c;
        var BodyRow = row || 'tr';
        var cells = [];
        var className = typeof rowClassName === 'function'
            ? rowClassName(data, rowIndex)
            : rowClassName;
        var height = fixed && fixedColumnsBodyRowsHeight[rowIndex]
            ? fixedColumnsBodyRowsHeight[rowIndex]
            : undefined;
        (columns || []).forEach(function (column, columnIndex) {
            var pos = {
                row: rowIndex,
                column: columnIndex,
                fixed: fixed,
            };
            cells.push(React.createElement(Cell, { column: column, data: data, pos: pos, columnIndex: columnIndex, key: columnIndex, prefix: prefix }));
        });
        return (React.createElement(BodyRow, __assign({ className: classnames(prefix + "-grid-tr", className, (_a = {},
                _a[prefix + "-grid-tr__mouseover"] = mouseOverRowIndex === rowIndex,
                _a)), onClick: function (e) { return onRowClick(data, rowIndex, e); }, onMouseEnter: function () { return scroll && scroll.x && onRowMouseEnter(rowIndex); }, style: { height: height } }, rowProps(data, rowIndex), { "data-zv": "7.4.3" }), cells));
    };
    return Row;
}(PureComponent));
export default Row;
