import * as React from 'react';
import cx from 'classnames';
import useDelayed from './hooks/useDelayed';
import Icon from './components/icons';
export function InlineLoading(props) {
    var _a = props.loading, loading = _a === void 0 ? false : _a, _b = props.delay, delay = _b === void 0 ? 0 : _b, className = props.className, _c = props.icon, icon = _c === void 0 ? 'youzan' : _c, iconSize = props.iconSize, iconText = props.iconText, _d = props.textPosition, textPosition = _d === void 0 ? 'bottom' : _d;
    var delayed = useDelayed({ loading: loading, delay: delay });
    if (delayed || !loading) {
        return null;
    }
    return (React.createElement("div", { className: cx('zent-loading', 'zent-loading--inline', className), "data-zv": '7.4.3' },
        React.createElement(Icon, { icon: icon, size: iconSize, text: iconText, textPosition: textPosition })));
}
export default InlineLoading;
