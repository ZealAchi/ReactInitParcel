import * as React from 'react';
import cx from 'classnames';
import useDelayed from './hooks/useDelayed';
import LoadingMask from './components/LoadingMask';
import { Portal } from '../portal';
var NO_STYLE = {};
export function FullScreenLoading(props) {
    var _a = props.loading, loading = _a === void 0 ? false : _a, _b = props.delay, delay = _b === void 0 ? 0 : _b, className = props.className, _c = props.icon, icon = _c === void 0 ? 'youzan' : _c, iconSize = props.iconSize, iconText = props.iconText, _d = props.textPosition, textPosition = _d === void 0 ? 'bottom' : _d, zIndex = props.zIndex;
    var delayed = useDelayed({ loading: loading, delay: delay });
    if (delayed || !loading) {
        return null;
    }
    var style = typeof zIndex === 'number' ? { zIndex: zIndex } : NO_STYLE;
    return (React.createElement(Portal, { className: cx('zent-loading', 'zent-loading--fullscreen', className), style: style, blockPageScroll: true },
        React.createElement(LoadingMask, { icon: icon, size: iconSize, text: iconText, textPosition: textPosition })));
}
export default FullScreenLoading;
