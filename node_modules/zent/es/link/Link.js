import { __assign, __rest } from "tslib";
import * as React from 'react';
import cx from 'classnames';
import { DisabledContext } from '../disabled';
export var Link = React.forwardRef(function (props, ref) {
    var disabledContext = React.useContext(DisabledContext);
    var className = props.className, _a = props.disabled, disabled = _a === void 0 ? disabledContext.value : _a, onClick = props.onClick, rest = __rest(props, ["className", "disabled", "onClick"]);
    return (React.createElement("a", __assign({}, rest, { className: cx(className, 'zent-link', {
            'zent-link__disabled': disabled,
        }), ref: ref, onClick: disabled ? preventOpenLink : onClick, "data-zv": '7.4.3' })));
});
function preventOpenLink(event) {
    event.preventDefault();
    event.stopPropagation();
}
