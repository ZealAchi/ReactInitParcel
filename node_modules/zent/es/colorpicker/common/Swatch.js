import * as React from 'react';
import reactCSS from '../helpers/reactcss';
export var Swatch = function (_a) {
    var color = _a.color, style = _a.style, onClick = _a.onClick, _b = _a.title, title = _b === void 0 ? color : _b;
    var styles = reactCSS({
        default: {
            swatch: {
                background: color,
                height: '100%',
                width: '100%',
                cursor: 'pointer',
            },
        },
        custom: {
            swatch: style,
        },
    }, 'custom');
    var handleClick = function (e) {
        onClick(color, e);
    };
    return React.createElement("div", { style: styles.swatch, onClick: handleClick, title: title, "data-zv": '7.4.3' });
};
export default Swatch;
