import flattenNames from './flattenNames';
import mergeClasses from './mergeClasses';
import autoprefix from './autoprefix';
export var ReactCSS = function (classes) {
    var activations = [];
    for (var _i = 1; _i < arguments.length; _i++) {
        activations[_i - 1] = arguments[_i];
    }
    var activeNames = flattenNames(activations);
    var merged = mergeClasses(classes, activeNames);
    return autoprefix(merged);
};
export default ReactCSS;
