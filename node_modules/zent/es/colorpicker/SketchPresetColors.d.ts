import * as React from 'react';
import { PresetColors, ColorPickerType } from '.';
export declare type SketchPresetColorValue = string | {
    hex: string;
    source: string;
};
export interface ISketchPresetColors {
    colors: PresetColors;
    onClick(color: SketchPresetColorValue, e?: React.MouseEvent<HTMLElement>): any;
    prefix: string;
    type: ColorPickerType;
}
declare const SketchPresetColors: ({ colors, onClick, prefix, type, }: ISketchPresetColors) => JSX.Element;
export default SketchPresetColors;
