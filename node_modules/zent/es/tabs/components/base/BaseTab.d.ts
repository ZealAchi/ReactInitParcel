import * as React from 'react';
import { Component } from 'react';
import { ITabProps } from '../../types';
declare abstract class BaseTab<Id> extends Component<ITabProps<Id>> {
    protected abstract typeName: string;
    get tabsCls(): string;
    onDel: (e: React.MouseEvent<HTMLSpanElement, MouseEvent>) => void;
    onClick: () => void;
    renderDelOperater(): JSX.Element;
}
export default BaseTab;
