export function getTabDataFromChild(child, activeId) {
    var _a = child.props, id = _a.id, disabled = _a.disabled, tab = _a.tab, panelChildren = _a.children, panelClassName = _a.className, unmountOnHide = _a.unmountOnHide;
    var props = {
        title: tab,
        disabled: disabled,
        key: id,
        actived: activeId === id,
        panelChildren: panelChildren,
        className: panelClassName,
        unmountOnHide: unmountOnHide,
    };
    return props;
}
export function commonTransformTabData(tabItem, candel) {
    return {
        key: tabItem.key,
        actived: tabItem.actived,
        disabled: tabItem.disabled,
        title: tabItem.title,
        className: tabItem.className,
        candel: candel && !tabItem.disabled,
    };
}
