import * as React from 'react';
import cx from 'classnames';
import { getLeft } from './common';
function isActive(value, left, right) {
    return value >= left && value <= right;
}
function Dots(_a) {
    var min = _a.min, max = _a.max, activeLeft = _a.activeLeft, activeRight = _a.activeRight, potentialValues = _a.potentialValues, disabled = _a.disabled;
    return (React.createElement(React.Fragment, null, potentialValues.map(function (value) { return (React.createElement("div", { key: value, className: cx('zent-slider-dot', {
            'zent-slider-dot-active': !disabled && isActive(value, activeLeft, activeRight),
        }), style: { left: getLeft(value, min, max) + "%" }, "data-zv": '7.4.3' })); })));
}
export default Dots;
