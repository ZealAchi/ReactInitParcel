import * as React from 'react';
export interface ISliderMarksProps {
    marks: Record<number | string, React.ReactNode>;
    min: number;
    max: number;
    potentialValues: number[];
}
declare const SliderMarks: React.MemoExoticComponent<({ marks, min, max, potentialValues }: ISliderMarksProps) => JSX.Element>;
export default SliderMarks;
