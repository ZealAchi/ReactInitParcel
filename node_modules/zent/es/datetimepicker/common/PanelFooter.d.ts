/// <reference types="react" />
import { noop } from '../constants';
declare const PanelFooter: {
    (props: any): JSX.Element;
    defaultProps: {
        onClickLink: typeof noop;
        onClickButton: typeof noop;
        showLink: boolean;
        showError: boolean;
        errorText: string;
    };
};
export default PanelFooter;
