import { Component } from 'react';
import Step from './components/Step';
export interface IStepsProps {
    type?: 'number' | 'card' | 'breadcrumb' | 'tabs';
    direction?: 'horizontal' | 'vertical';
    current?: number;
    status?: 'process' | 'finish' | 'error' | 'wait';
    sequence?: boolean;
    onStepChange?: (stepIndex: number) => void;
    className?: string;
    prefix?: string;
}
export declare class Steps extends Component<IStepsProps> {
    static defaultProps: {
        className: string;
        prefix: string;
        type: string;
        current: number;
        direction: string;
        size: string;
        status: string;
        sequence: boolean;
    };
    static Step: typeof Step;
    render(): JSX.Element;
}
export default Steps;
