import * as React from 'react';
import { PureComponent } from 'react';
export interface ISwiperDotsProps {
    prefix?: string;
    dotsColor: string;
    dotsSize?: 'normal' | 'small' | 'large';
    items: React.ReactNode;
    currentIndex: number;
    onDotsClick(index: number): void;
}
export default class SwiperDots extends PureComponent<ISwiperDotsProps> {
    isDotActive: (index: number, currentIndex: number, length: number) => boolean;
    isBuildInColor: (dotsColor: string) => boolean;
    render(): JSX.Element;
}
