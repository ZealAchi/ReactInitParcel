import { MediaQuery } from './MediaQuery';
import { IEnquireOptions } from './types';
export declare class MediaQueryDispatch {
    browserIsIncapable: boolean;
    queries: Record<string, MediaQuery>;
    constructor();
    register(q: string, options: IEnquireOptions | IEnquireOptions[] | (() => void), shouldDegrade?: boolean): this;
    unregister(q: string, handler?: IEnquireOptions | (() => void)): this;
}
