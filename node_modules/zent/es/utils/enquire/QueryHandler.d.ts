import { IEnquireOptions } from './types';
export declare class QueryHandler {
    initialized: boolean;
    options: IEnquireOptions;
    constructor(options: IEnquireOptions);
    setup(): void;
    on(): void;
    off(): void;
    destroy(): void;
    equals(target: IEnquireOptions | (() => void)): boolean;
}
