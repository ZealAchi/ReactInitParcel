import { format } from './fecha';
import { zhCN } from '../../i18n/time-locale';
import { getValidDate } from './helpers';
export default function formatDate(date, mask, locale) {
    if (mask === void 0) { mask = 'default'; }
    if (locale === void 0) { locale = zhCN; }
    var val = getValidDate(date);
    return format(val, mask, locale);
}
