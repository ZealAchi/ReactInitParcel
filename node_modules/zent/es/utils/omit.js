export default function omit(obj, paths) {
    if (obj === null || obj === undefined) {
        return {};
    }
    return Object.keys(obj).reduce(function (acc, k) {
        if (paths.indexOf(k) === -1) {
            acc[k] = obj[k];
        }
        return acc;
    }, {});
}
