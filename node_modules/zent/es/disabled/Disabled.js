import * as React from 'react';
export var DisabledContext = React.createContext({
    value: false,
});
DisabledContext.displayName = 'DisabledContext';
export var Disabled = function (_a) {
    var _b = _a.value, value = _b === void 0 ? true : _b, children = _a.children;
    var ctx = React.useMemo(function () { return ({
        value: value,
    }); }, [value]);
    return (React.createElement(DisabledContext.Provider, { value: ctx }, children));
};
