import { __assign } from "tslib";
import * as React from 'react';
import DateRangeQuickPicker from '../../date-range-quick-picker';
import { dateRangeDefaultValueFactory } from '../shared';
import { FormField } from '../Field';
export var FormDateRangeQuickPickerField = function (props) {
    return (React.createElement(FormField, __assign({}, props, { defaultValue: props
            .defaultValue || dateRangeDefaultValueFactory }), function (childProps) {
        var _a = React.useState(undefined), chosenDays = _a[0], setChosenDays = _a[1];
        var onChange = React.useCallback(function (value, chosenDays) {
            childProps.onChange(value);
            setChosenDays(chosenDays);
        }, [childProps.onChange]);
        return (React.createElement(DateRangeQuickPicker, __assign({}, props.props, childProps, { chooseDays: chosenDays, onChange: onChange })));
    }));
};
