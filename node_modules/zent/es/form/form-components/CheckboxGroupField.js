import { __assign } from "tslib";
import * as React from 'react';
import { CheckboxGroup } from '../../checkbox';
import { FormField } from '../Field';
function renderCheckboxGroup(childProps, props) {
    return (React.createElement(CheckboxGroup, __assign({}, props.props, childProps), props.children));
}
var DEFAULT_VALUE = [];
export function FormCheckboxGroupField(props) {
    return (React.createElement(FormField, __assign({}, props, { defaultValue: props.defaultValue ||
            DEFAULT_VALUE }), function (childProps) { return renderCheckboxGroup(childProps, props); }));
}
