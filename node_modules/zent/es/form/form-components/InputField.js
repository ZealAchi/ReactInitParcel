import { __assign } from "tslib";
import * as React from 'react';
import Input from '../../input';
import { FormField } from '../Field';
import { TouchWhen, ValidateOccasion } from '../shared';
function renderInput(childProps, props) {
    var onChange = React.useCallback(function (e) {
        childProps.onChange(e.target.value);
    }, [childProps.onChange]);
    return (React.createElement(Input, __assign({}, props.props, childProps, { onChange: onChange })));
}
export var FormInputField = function (props) {
    var _a = props.validateOccasion, validateOccasion = _a === void 0 ? ValidateOccasion.Blur : _a;
    return (React.createElement(FormField, __assign({}, props, { defaultValue: props.defaultValue || '', touchWhen: TouchWhen.Blur, validateOccasion: validateOccasion }), function (childProps) { return renderInput(childProps, props); }));
};
