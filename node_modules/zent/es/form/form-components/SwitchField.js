import { __assign, __rest } from "tslib";
import * as React from 'react';
import Switch from '../../switch';
import { FormField } from '../Field';
function renderSwitch(childProps, props) {
    var value = childProps.value, passedProps = __rest(childProps, ["value"]);
    return React.createElement(Switch, __assign({}, props.props, passedProps, { checked: value }));
}
export var FormSwitchField = function (props) {
    return (React.createElement(FormField, __assign({}, props, { defaultValue: typeof props.defaultValue ===
            'boolean'
            ? props.defaultValue
            : false }), function (childProps) { return renderSwitch(childProps, props); }));
};
