import { __assign } from "tslib";
import * as React from 'react';
import NumberInput from '../../number-input';
import { FormField } from '../Field';
export var FormNumberInputField = function (props) {
    return (React.createElement(FormField, __assign({}, props, { defaultValue: props.defaultValue || '' }), function (childProps) { return React.createElement(NumberInput, __assign({}, props.props, childProps)); }));
};
