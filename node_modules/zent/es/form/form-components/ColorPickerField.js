import { __assign, __rest } from "tslib";
import * as React from 'react';
import ColorPicker from '../../colorpicker';
import { FormField } from '../Field';
function renderColorPicker(childProps, props) {
    var value = childProps.value, passedProps = __rest(childProps, ["value"]);
    return React.createElement(ColorPicker, __assign({}, props.props, passedProps, { color: value }));
}
export var FormColorPickerField = function (props) {
    return (React.createElement(FormField, __assign({}, props, { defaultValue: props.defaultValue || '' }), function (childProps) { return renderColorPicker(childProps, props); }));
};
