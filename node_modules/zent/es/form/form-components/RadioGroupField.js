import { __assign } from "tslib";
import * as React from 'react';
import { RadioGroup } from '../../radio';
import { FormField } from '../Field';
function renderRadioGroup(childProps, props) {
    var onChange = React.useCallback(function (e) {
        childProps.onChange(e.target.value);
    }, [childProps.onChange]);
    return (React.createElement(RadioGroup, __assign({}, props.props, childProps, { onChange: onChange }), props.children));
}
export function FormRadioGroupField(props) {
    return (React.createElement(FormField, __assign({}, props, { defaultValue: 'defaultValue' in props ? props.defaultValue : null }), function (childProps) { return renderRadioGroup(childProps, props); }));
}
