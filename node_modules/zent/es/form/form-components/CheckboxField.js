import { __assign, __rest } from "tslib";
import * as React from 'react';
import Checkbox from '../../checkbox';
import { FormField } from '../Field';
function renderCheckbox(childProps, props) {
    var value = childProps.value, passedProps = __rest(childProps, ["value"]);
    var onChange = React.useCallback(function (e) {
        childProps.onChange(e.target.checked);
    }, [childProps.onChange]);
    return (React.createElement(Checkbox, __assign({}, props.props, passedProps, { checked: value, onChange: onChange })));
}
export function FormCheckboxField(props) {
    return (React.createElement(FormField, __assign({}, props, { defaultValue: props.defaultValue ||
            false }), function (childProps) { return renderCheckbox(childProps, props); }));
}
