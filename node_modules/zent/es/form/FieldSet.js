import * as React from 'react';
import { useFieldSet, FormProvider, useValue$, } from 'formulr';
import { defaultRenderError, asFormChild, } from './shared';
export function FieldSet(props) {
    var _a = useFieldSet(props.name || props.model, props.validators), ctx = _a[0], model = _a[1];
    var scrollAnchorRef = props.scrollAnchorRef, _b = props.renderError, renderError = _b === void 0 ? defaultRenderError : _b;
    asFormChild(model, scrollAnchorRef);
    useValue$(model.error$, model.error$.getValue());
    return (React.createElement(FormProvider, { value: ctx },
        props.children,
        renderError(model.error)));
}
