import { useState, useEffect } from 'react';
export function useValue$(value$, initialValue) {
    var _a = useState(initialValue), value = _a[0], setValue = _a[1];
    useEffect$(value$, function (value) {
        setValue(value);
    });
    return value;
}
export function useEffect$(event$, effect) {
    useEffect(function () {
        var $ = event$.subscribe(effect);
        return function () { return $.unsubscribe(); };
    }, [event$]);
}
