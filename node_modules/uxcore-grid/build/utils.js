'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _StyleMaps = require('./StyleMaps');

var _StyleMaps2 = _interopRequireDefault(_StyleMaps);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function isGridProp(propName) {
  var field = ['Pull', 'Offset', 'Push'];
  var styleKeys = Object.keys(_StyleMaps2["default"].SIZES);
  var styleValues = [];
  for (var i = 0; i < styleKeys.length; i++) {
    styleValues.push(_StyleMaps2["default"].SIZES[styleKeys[i]]);
  }
  var len = styleValues.length;
  var gridProps = [].concat(styleValues);
  var state = false;
  field.forEach(function (value) {
    for (var _i = 0; _i < len; _i++) {
      gridProps.push(styleValues[_i] + value);
    }
  });

  gridProps.forEach(function (value) {
    if (value === propName) {
      state = true;
    }
  });
  return state;
}

function splitGridProps(props) {
  var elementProps = [];
  var getGridProps = {};
  var propName = Object.keys(props);
  for (var i = 0; i < propName.length; i++) {
    if (!isGridProp(propName[i])) {
      elementProps[propName[i]] = props[propName[i]];
    } else {
      getGridProps[propName[i]] = props[propName[i]];
    }
  }
  return [elementProps, getGridProps];
}

exports["default"] = { splitGridProps: splitGridProps };
module.exports = exports['default'];